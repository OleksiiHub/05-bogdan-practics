// function Answers({ incorrectAnswers, correctAnswer }) {
//     const arrayConcat = incorrectAnswers.concat(correctAnswer)
//     const arrayRandom = arrayConcat.sort(() => Math.random() - 0.5);
//     return arrayRandom
// }

// export default Answers











